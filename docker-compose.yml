version: '3.9'

services:
  backend:
    build: ./Backend
    ports:
      - "8080:8080"
    volumes:
      - ./Backend:/app
    restart: always

  frontend:
    build: ./Frontend
    ports:
      - "3000:3000"
    volumes:
      - ./Frontend:/app
    restart: always